set -gx LEDGER_FILE "$HOME/Sync/Workspace/hledger_tests/test_ledger.journal"
abbr -a hl hledger
abbr -a hlb "hledger bal"
abbr -a hlr "hledger reg"
abbr -a hls "hledger setup"
abbr -a hlc "hledger check"
